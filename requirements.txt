# Reference environment: Python 3.11 in the TensorFlow 2.20 GPU Jupyter image.
# Pins match the installed environment used for project compatibility checks.
# Linux x86_64 GPU libraries are supplied by TensorFlow's and-cuda extra;
# the NVIDIA host driver and Docker GPU access must be configured separately.
tensorflow[and-cuda]==2.20.0 ; platform_system == "Linux" and platform_machine == "x86_64"
tensorflow==2.20.0 ; platform_system != "Linux" or platform_machine != "x86_64"
keras==3.11.2
tensorboard==2.20.0
numpy==2.3.2
h5py==3.14.0
ml-dtypes==0.5.3
protobuf==6.31.1

# Data loading, evaluation, configuration, optimization, and resource reporting.
pandas==3.0.5
scipy==1.17.1
scikit-learn==1.9.1
PyYAML==6.0.2
optuna==5.0.0
psutil==7.0.0

# Figures, generated images, and notebook display.
matplotlib==3.10.5
Pillow==11.3.0
ipython==9.4.0

# Local notebook server and the kernel configured by .devcontainer.
ipykernel==6.30.1
jupyterlab==4.4.5
notebook==7.4.5
nbformat==5.10.4

# The separate PyTorch/Avalanche demo in notebooks/test.ipynb additionally
# needs torch and avalanche-lib; they are not used by the TensorFlow thesis.
