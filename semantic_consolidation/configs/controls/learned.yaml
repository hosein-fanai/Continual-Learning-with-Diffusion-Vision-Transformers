# Standalone CIFAR-10 development control; prepare paired runs with controls.py.
base_config: ../common_v1.yaml
route:
  condition: learned
  acquisition_steps: 100
  consolidation_steps: 100
  batch_size: 32
  noise_levels: [0]
