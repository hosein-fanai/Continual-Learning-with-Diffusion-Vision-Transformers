# Example bands for validation selection; share these levels with noisy_weighted.
base_config: ../common_v1.yaml
route:
  condition: learned
  acquisition_steps: 100
  consolidation_steps: 100
  batch_size: 32
  noise_levels: [0, 50, 150]
  reliability: uniform
