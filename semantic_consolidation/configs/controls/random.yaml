# Identical initialized-gate distribution and updates; acquisition rate is zero.
base_config: ../common_v1.yaml
route:
  condition: random
  acquisition_steps: 100
  consolidation_steps: 100
  batch_size: 32
  noise_levels: [0]
